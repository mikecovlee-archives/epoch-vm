#pragma once

#include <string>
#include <cstdint>

namespace epoch {
	using vm_int = long;
	using vm_float = double;
	using vm_bool = bool;
	using vm_char = signed char;
	using vm_string = std::string;
} // namespace epoch
